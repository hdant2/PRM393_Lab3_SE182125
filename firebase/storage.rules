rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // PDF report — chỉ user đã login upload/đọc file của chính mình
    match /reports/{userId}/{fileName} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
